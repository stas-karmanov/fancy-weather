export const config = {
    GEOLOCATION_API_KEY,
    WEATHER_API_KEY,
    IMAGES_API_KEY,
    MAPS_API_KEY,
    GEOCODING_API_KEY,
    // eslint-disable-next-line no-undef
    ENV: process.env.NODE_ENV,
};
