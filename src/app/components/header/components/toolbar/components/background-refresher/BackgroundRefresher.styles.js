import { createUseStyles } from 'react-jss';

export const useStyles = createUseStyles({
    refresher: {
        backgroundColor: 'rgba(174, 181, 185, 0.8)',
        width: 44,
        height: 44,
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        borderRadius: 5,
        cursor: 'pointer',
    },
});
