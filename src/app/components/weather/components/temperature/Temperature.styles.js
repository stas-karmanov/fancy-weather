import { createUseStyles } from 'react-jss';

export const useStyles = createUseStyles({
    temperatureWrapper: {
        display: 'flex',
    },
    temperature: {
        fontSize: 306,
    },
    degreesSign: {
        fontSize: 100,
        paddingTop: 30,
    },
});
