import { createAction } from '../../../store/create-action';

export const setForecast = createAction('[Weather] Set Weather Forecast');
export const setLocation = createAction('[Weather] Set Weather Location');
export const setWeather = createAction('[Weather] Set Weather');
